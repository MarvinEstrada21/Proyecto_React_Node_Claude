# Vacio a proposito: el frontend detecta automaticamente la IP/host del
# backend usando el mismo host con el que se abrio la pagina (ver
# src/api/client.js). Solo completa esto si necesitas forzar una URL fija.
VITE_API_URL=
