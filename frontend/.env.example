# Opcional. Si se deja vacio, el frontend usa automaticamente el mismo host
# con el que abriste la pagina (localhost, la IP de la red, etc.) y el
# puerto 4000. Completalo solo si el backend corre en otro puerto/host fijo.
VITE_API_URL=
